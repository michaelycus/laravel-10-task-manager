APP_KEY=base64:RsVXeXhzG7sni9EWRvfcfvhfzH90IN2Ey+loT46PvU8=

APP_ENV=testing
APP_DEBUG=false # Typically false for testing
BCRYPT_ROUNDS=4 # Speeds up hashing
CACHE_DRIVER=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync # Run jobs immediately
MAIL_MAILER=array

DB_CONNECTION=pgsql
DB_HOST=pgsql
DB_PORT=5432
DB_DATABASE=testing # IMPORTANT: Use a separate test database
DB_USERNAME=sail
DB_PASSWORD=password

# If using Laravel Echo for real-time (Bonus)
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

# VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
# VITE_PUSHER_HOST="${PUSHER_HOST}"
# VITE_PUSHER_PORT="${PUSHER_PORT}"
# VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
# VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
